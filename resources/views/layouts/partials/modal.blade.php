<div class="modal fade" id="{{ $idModal ? $idModal : 'modal-form' }}" role="dialog" aria-labelledby="myModalForm" data-backdrop="static">
    <div class="modal-dialog {{ isset($typeModal) ? $typeModal : 'modal-md' }}" role="document">
        <div id="{{ $idContent ? $idContent : 'content-modal' }}" class="modal-content">

        </div>
    </div>
</div>
